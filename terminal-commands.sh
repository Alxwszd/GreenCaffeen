# 确保您在项目根目录下
cd path/to/reusablecups_system

# 如果尚未初始化Git仓库
git init

# 添加所有文件到暂存区
git add .

# 提交更改
git commit -m "Initial commit"

# 添加远程仓库
git remote add origin https://github.com/Alxwszd/GreenCaffeen.git

# 推送到GitHub
git push -u origin master
# 或者如果您使用main作为默认分支
# git push -u origin main